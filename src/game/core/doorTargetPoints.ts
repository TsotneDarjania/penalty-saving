import { FootballDor } from "../../gameObjects/footballDor";

export class DorTargetPoints {
  constructor(public FootballDor: FootballDor) {
    this.calculateTargetPoints();
  }

  calculateTargetPoints() {}
}
